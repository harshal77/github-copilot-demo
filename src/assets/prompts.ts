export class Prompts {
    public static readonly prompts = `
    create a reactive form which accept the user first name , lastname age and phonenumber using mat input 
    Create a reactive form  which will accept a user first name last name age and mobile number also first name and last name is mandatory.
    Define a method called submitForm that, upon determining that the form is valid, creates a new object with a dynamic ID and closes the popup.
    Declare in the constructor and add imports to make this component appropriate as a mat dailog.
    Include a close popup feature and direct the new user to it.
    add editUser and deleteUser methods. Edit user method open the same popup with prefilled value and delete should delete the reocrd from table
    Add data as a input to the mat dailog in constructor and if data is present the assign user form values
    `

}